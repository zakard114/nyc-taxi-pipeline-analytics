# Kestra image + Docker CLI so shell flows can `docker run` against the mounted host socket.
FROM kestra/kestra:latest
USER root
RUN apt-get update \
  && apt-get install -y --no-install-recommends docker.io \
  && rm -rf /var/lib/apt/lists/*
