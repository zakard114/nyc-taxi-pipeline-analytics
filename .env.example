# Copy to .env and set your Kestra UI login (email + password)
# Used for persistent auth - no more re-creating account on each restart

KESTRA_USER=your-email@example.com
KESTRA_PASSWORD=YourSecurePassword8
